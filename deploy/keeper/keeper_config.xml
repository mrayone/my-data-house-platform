<!--
  Keeper single-node para a PoC (ADR-0008: topologia single-node por
  desenho). Substitui ZooKeeper; serve tanto a coordenação de réplicas
  (não usada nesta PoC single-node) quanto o estado de KeeperMap (ADR-0002).
-->
<clickhouse>
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>

        <log_storage_path>/var/lib/clickhouse-keeper/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse-keeper/snapshots</snapshot_storage_path>

        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
            <raft_logs_level>information</raft_logs_level>
        </coordination_settings>

        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>clickhouse-keeper</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>

    <listen_host>0.0.0.0</listen_host>
</clickhouse>
